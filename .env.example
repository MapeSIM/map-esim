# VeSIM broker (server-only — never expose to the client)
VESIM_BASE_URL=
VESIM_EMAIL=
VESIM_PASSWORD=

# Customer order email delivery (server-only)
# Leave unset / empty to skip sending — orders still succeed with emailDelivery=not_configured
EMAIL_PROVIDER=
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
