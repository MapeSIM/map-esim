import AdminPlaceholder from "@/app/components/admin/AdminPlaceholder";

export const dynamic = "force-dynamic";

export default function AdminOrdersPage() {
  return (
    <AdminPlaceholder
      title="Orders"
      description="Order search, filters, and safe provider-status refresh arrive in Phase 3C. No refund, cancel, or email actions are available yet."
    />
  );
}
